name: 'payment_reconciliation'
version: '1.0.0'

profile: 'payment_reconciliation'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

models:
  payment_reconciliation:
    raw:
      +schema: raw
    staging:
      +schema: staging
